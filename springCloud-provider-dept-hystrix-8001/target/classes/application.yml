server:
  port: 8001

mybatis:
  type-aliases-package: com.syx.springCloud.pojo
  config-location: classpath:mybatis/mybatis-config.xml
  mapper-locations: classpath:mybatis/mapper/*.xml

spring:
  application:
    name: springCloud-provider-dept
  datasource:
    type: com.alibaba.druid.pool.DruidDataSource
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://47.113.218.78:3306/db01?useUnicode=true&characterEncoding=utf-8
    username: root
    password: syxsyx

# 注册自己的服务
eureka:
  client:
    service-url:
      defaultZone: http://localhost:7001/eureka/
  instance: # 配置默认描述
    instance-id: springCloud-provider-dept-8001-hystrix

info: #actuator
  app.name: syx-springCloud
  company.name: syx
